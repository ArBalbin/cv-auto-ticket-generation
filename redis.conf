# QueueFlow Redis cache configuration for self-hosted Redis.
#
# QueueFlow uses Redis only as a temporary live-state/snapshot cache.
# Keep Redis in-memory only so annotated camera snapshots are not persisted
# to Redis RDB or AOF files on disk.

# Disable RDB snapshot persistence.
save ""

# Disable append-only file persistence.
appendonly no
